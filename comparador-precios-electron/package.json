{
  "name": "precio-claro",
  "version": "1.0.0",
  "description": "Catálogo local de productos ordenado por las mejores ofertas encontradas en la web.",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/store.js && node --check src/providers/serpapi.js && node --check src/renderer.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "electron": "^37.0.0"
  }
}
