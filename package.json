{
  "name": "nexusdata",
  "version": "1.0.0",
  "description": "Centro de información técnica para desarrollo de software",
  "main": "desktop/main.js",
  "private": true,
  "scripts": {
    "dev": "electron desktop/main.js",
    "start": "electron desktop/main.js",
    "server": "node server/app.js",
    "test-source": "node test-source-server/server.js",
    "db:up": "docker compose up -d mysql",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f mysql",
    "test": "node --test",
    "check": "node --check server/app.js && node --check server/auth.js && node --check server/database/mysql.js && node --check server/services/html-viewer.js && node --check server/services/html-viewer-preview.js && for file in server/services/code-map/*.js server/services/code-map/analyzers/*.js server/services/search/*.js; do node --check \"$file\"; done && node --check test-source-server/server.js && node --check desktop/main.js && node --check desktop/preload.js && node --check desktop/js/markdown.js && node --check desktop/js/app.js && node --check desktop/js/views/diagram-language.mjs && for file in desktop/js/core/*.js desktop/js/ui/*.js desktop/js/views/*.js; do node --check \"$file\"; done",
    "build:mac": "electron-builder --mac dmg --universal",
    "build:win": "electron-builder --win nsis --x64"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.5.2",
    "fuse.js": "^7.1.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.6"
  },
  "devDependencies": {
    "electron": "^37.0.0",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.nexusdata.desktop",
    "productName": "NexusData",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "desktop/**/*",
      "server/**/*",
      "package.json",
      "!test{,/**}",
      "!test-source-server{,/**}",
      "!docs{,/**}",
      "!ejemplo-nexusdata{,/**}",
      "!SDD_specs{,/**}",
      "!specs.md",
      "!implementaciones_nuevas.txt",
      "!.env",
      "!compose.yaml"
    ],
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "mac": {
      "category": "public.app-category.productivity",
      "identity": null
    },
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
